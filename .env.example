# Example .env for job_finder_bot
# Copy to .env and add your own token
DISCORD_TOKEN=your_discord_bot_token_here

# Database connection (for local development without Docker)
# When using docker-compose, this is set automatically
DATABASE_URL=postgresql://jobfinder:secret@localhost:5432/jobfinder

